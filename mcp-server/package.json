{
  "name": "routeforce-mcp-server",
  "version": "1.0.0",
  "description": "Model Context Protocol server for RouteForce route optimization",
  "main": "build/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "start:filesystem": "node build/filesystem.js",
    "dev": "tsc && node build/index.js",
    "dev:filesystem": "tsc && node build/filesystem.js",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mcp",
    "routeforce",
    "routing",
    "optimization",
    "logistics"
  ],
  "author": "RouteForce Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.2.1",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.2",
    "zod": "^3.25.76"
  }
}
